

// Client

--Commands--
V:      = Delta command for adding or updating a delta
RV:     = Return a delta from the cluster map

Header Requirements:


--Responses--
ROUTE:  = A route response from the node specifying to re-connect to another node
CT:     = Connected to response - specifying what node and addr the connection has been established on
//Eventually want location specific and regional cluster specific